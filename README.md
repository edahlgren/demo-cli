#### Launch a demo container:

```
$ demo up -f ~/demo-mag-v1.1/ant-search/Demofile
```

#### Bring it down:

```
$ demo down -f ~/demo-mag-v1.1/ant-search/Demofile
```

#### Get shell access to an 'up' demo:

```
$ demo shell -f ~/demo-mag-v1.1/ant-search/Demofile
```

